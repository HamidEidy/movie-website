/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains:[
            "moviesapi.ir",
            "dummyimage.com"
        ],
    },

};

export default nextConfig;
